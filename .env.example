# Local dev: point at the .NET API (default when running dotnet run on port 5299).
VITE_API_BASE_URL=http://localhost:5299/api/quiz

# Production / remote API (set at build time on Render):
# VITE_API_BASE_URL=https://claudetest-be.onrender.com/api/quiz
